name: velik
description: "A fun virtual cycling playground."
publish_to: 'none'
version: 1.0.2

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

dependencies:
  flutter:
    sdk: flutter

  flame: ^1.37.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  very_good_analysis: ^10.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/parallax/

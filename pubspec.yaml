name: velik
description: "A fun virtual cycling playground, early-alpha with bad graphics and questionable physics. WIP"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0
  flutter: ^3.35.0

dependencies:
  flutter:
    sdk: flutter

  flame: ^1.32.0
  flutter_blue_plus: ^1.36.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  very_good_analysis: ^9.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/parallax/_orig/
    - assets/images/parallax/Island/
    - assets/images/parallax/Forest/
    - assets/images/parallax/RTB/
    - assets/images/parallax/TheDawn/
